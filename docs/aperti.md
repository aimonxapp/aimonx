# Aperti — la lista viva del sito

> ➡️ **Qui c'è *cosa* è aperto. *In che ordine* si chiude sta in [`docs/piano-chiusura.md`](piano-chiusura.md)** — ⛔ **questo registro non è un piano e non stabilisce un ordine.**
>
> ⭐ **Questa è la lista viva degli aperti del sito: si aggiorna qui, e una voce si chiude quando un giro la chiude.**
>
> ⚠️ **Ricalcato dal registro dell'app** (`~/Developer/AIMONX/docs/aperti.md`, il preambolo di `## 0.B`) — regola *«il sito ricalca, non riscrive»*. Le tre regole e le legende sono le stesse; cambiano gli ID e i percorsi.

## Registro unico degli aperti

> ⛔ **REGOLE DEL REGISTRO, e sono tre.**
> 1. **Questa è l'unica lista completa** degli aperti del sito. Le fonti restano l'autorità sul *merito* — `spec-sito.md` per i contenuti, `prodotto.md` dell'app per ciò che si può promettere; il registro dice solo **cosa è aperto, dove sta scritto, chi ce l'ha in mano, se blocca.**
> 2. ⛔ **Gli ID non si riusano e non si rinumerano MAI.** ⚠️ **Il prefisso `W` (= web) non è decorativo:** distingue una voce del sito da una dell'app a colpo d'occhio, in un progetto dove i due registri si citano a vicenda. Una voce chiusa resta in tabella, barrata, con ✅ e l'indirizzo di chi l'ha chiusa. Un registro che si accorcia perde la memoria di ciò che è stato risolto e come.
> 3. ⛔ **Prima di scrivere o riscrivere una voce, si apre la fonte.** Per una voce **gemella** la fonte è la voce nel registro dell'app, e si apre lì, non a memoria: le sei gemelle di questo registro sono state scritte dopo aver letto `RD16`, `RA4`, `RA1`, `RA2`, `RA3`, `RA11` in `~/Developer/AIMONX/docs/aperti.md`.
>
> ⭐ **La regola delle gemelle, e vale nei due versi.** Una voce del registro dell'app che riguarda il sito **resta aperta anche là**: la gemella qui **non la sostituisce e non la chiude.** La gemella porta lo **stesso numero** della sua (`WA1 ↔ RA1`), e ⛔ **le voci nate qui partono da numeri che nessuna gemella userà** — `WA15` in avanti, `WD17` in avanti — perché importare una gemella nuova non deve mai collidere con una voce di casa. **Chi chiude una voce da una parte lo scrive in `bacheca.md`.**
>
> **Legenda «Blocca?»** — ⛔ **SÌ**: senza questo il sito non si pubblica, o la submission dell'app non parte · ⚠️ **da accertare**: dipende da una verifica esterna o da una decisione non presa · **—**: non blocca.
> **Legenda «Mano»** — **P** = Pier · **CW** = Cowork · **CC** = Claude Code · **P+CW** = decide Pier, scrive Cowork.
> **Legenda dello stato** — ✅ **chiusa**, e allora la riga *Cosa* è **barrata** (regola 2) · ⏳ **aperta** · 🔒 **non si corregge, per scelta**: non aspetta nessuno, e ⛔ **non è un aperto**.

### A — blocca la pubblicazione

| ID | Cosa | Dove | Mano | Stato | Blocca? |
|---|---|---|---|---|---|
| **WA1** | **Privacy Policy online** — la pagina vive sul sito, e senza di essa la submission dell'app non parte | gemella di **`RA1`** (`~/Developer/AIMONX/docs/aperti.md`, sezione A) · il testo lo scrive Cowork, `spec-sito.md` §8 la prevede in `/privacy-policy/` | P+CW | ⏳ non iniziata — ⛔ **il posto dove vivere non c'è ancora**: il sito pubblica il README | ⛔ SÌ |
| **WA2** | **Support URL online** — stessa riga di `WA1` nella fonte dell'app | gemella di **`RA2`** · `spec-sito.md` §8 la prevede in `/contatti/` | P+CW | ⏳ non iniziato | ⛔ SÌ |
| **WA3** | **Terms of Service** — ⚠️ **obbligatori o no non è accertato:** nella fonte dell'app §64.5 li elenca **senza** dirli obbligatori, §66.24.6 li mette **fra gli obbligatori**, e nessuna delle due cita una fonte Apple | gemella di **`RA3`** · `spec-sito.md` §8 li prevede in `/termini/` | CW | ⏳ non iniziati, e la domanda che li precede è aperta | ⚠️ **da accertare** — le due sezioni della fonte non dicono la stessa cosa |
| **WA4** | **Il contenuto del sito** — ⭐ **la catena tecnica è in piedi e misurata in questo giro:** `https://aimonx.app` risponde 200, servita da GitHub Pages con Jekyll 3.10.0, dominio collegato, certificato approvato. ⛔ **Ma la pagina pubblicata è il README del repo** — titolo *«aimonx-website»*, corpo *«AIMONX website»*: **nessuna landing e nessuna pagina di contenuto.** ⭐ **Il baricentro non è costruire la catena: è scrivere e pubblicare le pagine**, cioè lavoro di testo | gemella di **`RA4`** · `spec-sito.md` §9 (set minimo per il primo rilascio) · misura: `curl -sS https://aimonx.app/` | P + CW | ⏳ aperta — ⚠️ **due cose la precedono**, e stanno in [`piano-chiusura.md`](piano-chiusura.md): il riallineamento di `spec-sito.md` a `prodotto.md`, e la scelta di `WD19` | ⛔ SÌ — finché non ci vivono sopra `WA1` e `WA2`, che sono il blocco vero |
| **WA11** | **Scheda App Store localizzata anche in italiano** (primaria = inglese USA) — ⚠️ **sta in questo registro perché il sito e la scheda condividono i testi e la lingua**, non perché il sito la pubblichi: la scheda si compila in App Store Connect | gemella di **`RA11`** · `spec-sito.md` §0 (inglese US nativo, italiano secondario) · §12.1 | P+CW | ⏳ non fatta | ⚠️ non dichiarata bloccante nella fonte |
| **WA15** | ~~**Il push verso `origin` non passa:** il repo è di `aimonxapp/aimonx`, la credenziale sul Mac è di `Pier974`, che qui ha `push: false`.~~ ✅ **Chiusa nel giro W1.** Pier ha scelto la **strada B** e ha fatto il `gh auth login` di `aimonxapp`. ⛔ **La prima misura del giro era SBAGLIATA, e va letta:** avevo concluso che `gh auth git-credential` servisse le credenziali richieste **per nome**, perché con `aimonxapp` non ancora loggato rispondeva vuoto. ⚠️ **Rimisurato a login fatto: risponde vuoto lo stesso.** Quel helper serve **solo l'account attivo**, e rifiuta se il nome chiesto è un altro. ⭐ **L'isolamento vero è un credential helper LOCALE al repo sito** che chiama `gh auth token --user aimonxapp` — che invece funziona a prescindere dall'attivo. Nessun token su disco, nessuna modifica globale, e `gh` resta attivo su `Pier974` per l'app | `git config --local --get-all credential.https://github.com.helper` · misura: `git push --dry-run origin fondazione-W0` → `[new branch]`, RC=0 | P ha fatto il login, CC il resto | ✅ **chiusa nel giro W1** | — |
| **WA16** | ~~**HTTPS non è forzato su `aimonx.app`.** Il certificato c'è ed è approvato, ma `"https_enforced": false` e `http://aimonx.app/` risponde 200 senza rimandare a HTTPS.~~ ✅ **Chiusa nel giro W1.** Pier ha messo la spunta *Enforce HTTPS* in Settings → Pages il 21/09/2026. ⭐ **Le due misure sono d'accordo, e servono entrambe:** l'API dice `https_enforced: true`, e il sito vero risponde `301` verso `https://aimonx.app/`. ⚠️ **Resta un residuo, ed è `WD23`:** la pagina continua a dichiararsi `http://` nel proprio `canonical`, perché quello lo scrive Jekyll, non GitHub | `gh api repos/aimonxapp/aimonx/pages --jq .https_enforced` → `true` · `curl -sSI http://aimonx.app/` → `301` | P | ✅ **chiusa nel giro W1** — la spunta l'ha messa Pier, la misura CC | — |

### D — tecnici, igiene, debiti di documento

| ID | Cosa | Dove | Mano | Stato | Blocca? |
|---|---|---|---|---|---|
| **WD16** | **Igiene del repo**: `.gitignore` e licenza. ⭐ **`.gitignore` scritto nel giro W1**, e non per ordine: serviva a WD17, perché tiene fuori dal repo i due file che descrivono il Mac di Pier. ⛔ **Resta la licenza, ed è di Pier:** un repo pubblico senza licenza è «tutti i diritti riservati» per difetto — può essere la scelta giusta, ma deve essere una scelta | gemella di **`RD16`** · `.gitignore` · misura: `git ls-files` | P | ⏳ **metà**: `.gitignore` ✅ fatto da CC nel giro W1 · licenza ⏳ non scelta | — |
| **WD17** | ~~**Il repo è PUBBLICO, e i file di lavoro ci finiscono al primo push.**~~ ✅ **Deciso da Pier il 21/09/2026 ed eseguito nel giro W1:** il repo resta pubblico, e dai file tracciati sono usciti i percorsi delle sue cartelle — `CLAUDE.md`, `docs/aperti.md`, `scripts/controprova.sh` e `.claude/settings.json` (`WD22`). Stanno in `scripts/percorsi-locali.sh` e `.claude/settings.local.json`, tenuti fuori da `.gitignore`. ⭐ **Quel che rende la decisione duratura non è la ripulitura, è il controllo:** la controprova conta `percorsi_pier` a ogni giro, e il motivo di ricerca è scritto in modo da non trovare sé stesso | `.gitignore` · `scripts/percorsi-locali.sh` · misura: `scripts/controprova.sh` → `percorsi_pier=0` | P ha deciso, CC ha eseguito | ✅ **chiusa nel giro W1** | — |
| **WD18** | **Il nome del repo è sbagliato in tre documenti, e in due modi diversi.** Il nome vero è **`aimonx`**, proprietario **`aimonxapp`**. Dicono altro: `RA4` e `RD16` del registro dell'app (*«`aimonx-website`»*), `spec-sito.md` §1 (*«`~/Developer/aimonx-web`»*), `metodo-aimonx.md` (*«nome da verificare»*, con le due candidate). ⭐ **Da dove viene l'equivoco: il README del repo si intitola `# aimonx-website`** — il titolo del README non è il nome del repo. ⚠️ **La cartella locale si chiama `aimonx-web` per una ragione tecnica, non per il nome:** su APFS `~/Developer/aimonx` **è** `~/Developer/AIMONX`, il repo dell'app | i tre documenti citati · misura: `git remote -v` → `https://github.com/aimonxapp/aimonx.git` | CW | ⏳ **aperta, trovata in questo giro** — ⛔ **i tre documenti stanno fuori dal repo: li corregge Cowork, non CC** | — |
| **WD19** | ~~**Con cosa si costruisce il sito** — Jekyll, HTML scritto a mano, un altro generatore statico.~~ ✅ **Deciso da Pier il 21/09/2026: Jekyll**, ed è anche ciò che Pages già faceva per difetto. Il codice lo scrive CC. ⛔ **Il criterio scritto qui prima era sbagliato, e va detto:** diceva *«quanto costa a Pier cambiare un testo»* — **ma Pier non scrive codice**, quindi quel costo non è suo e non misura niente. ⭐ **I costi si misurano sul lavoro dei giri e sul rischio**, cioè quante sessioni serve per fare una cosa e cosa succede se va storta. ⭐ **Primo pezzo fatto nel giro W1:** `_config.yml` sul ramo, che esclude i file di lavoro dalle pagine e dichiara `url: https://aimonx.app`. ⛔ **Nessun tema, nessuna pagina: vengono con `WA4`** | `_config.yml` · `spec-sito.md` §6 · misura: `curl -sS https://aimonx.app/ \| grep generator` → `Jekyll v3.10.0` | P | ✅ **decisa nel giro W1 da Pier** — resta da provare l'esclusione, vedi `WD25` | — |
| **WD20** | **La skill `/giro` è una COPIA, non un collegamento, e il master comune non esiste.** ⚠️ **Misurato:** `…/10- AIMONX AI Common/` contiene due file — `bacheca.md` e `metodo-aimonx.md` — e **nessuna cartella `skills/`**. ⭐ **La misura che serve alla scelta futura, fatta in questo giro:** Claude Code carica le skill leggendo `.claude/skills/*/SKILL.md`, e ⚠️ **se `giro/` fosse un collegamento simbolico a una cartella fuori dal repo, il caricamento dipenderebbe da come il client risolve i link — non è provato qui e non va dato per scontato.** ⛔ **Una prova vale più di una previsione:** quando il master esisterà, si prova il collegamento in un ramo usa-e-getta e si guarda se `/giro` compare. ⭐ **La scelta fra copia e collegamento resta a Pier** | `.claude/skills/giro/SKILL.md` · misura: `find "…/10- AIMONX AI Common"` | P | ⏳ **aperta, trovata in questo giro** — ⚠️ **finché è una copia, chi tocca una delle due skill lascia una riga in `bacheca.md`** | — |
| **WD21** | **`…/8- AIMONX web/4- risorse/` esiste ed è VUOTA**, e `spec-sito.md` §1 la dà per la destinazione degli asset grafici. ⚠️ **Oggi l'unico materiale grafico del sito sta in `2- Contenuti/TEST home landing page/HOME AIMONX.png`**, cioè non dove la spec dice. ⭐ **La prima misura di questo giro diceva *«non esiste»*, ed era sbagliata:** `find … -type f` non elenca le cartelle vuote. **L'ha trovata la controprova**, non una rilettura | `spec-sito.md` §1 · misura: `find "…/8- AIMONX web" -type f` | CW | ⏳ **aperta, trovata in questo giro** — ⛔ **la cartella sta fuori dal repo: la crea Cowork, non CC** | — |
| **WD22** | ~~**`.claude/settings.json` è tracciato e contiene quindici percorsi del disco di Pier, e CC non può riscriverlo** — il classificatore *auto mode* di Claude Code gli nega di modificare la propria configurazione (*Self-Modification*).~~ ✅ **Chiusa nel giro W1**, dopo che Pier ha autorizzato la scrittura su `.claude/`. Il blocco `permissions.deny` è in `.claude/settings.local.json`, che `.gitignore` tiene fuori; `settings.json` resta tracciato con hook e skill. ⭐ **E il recinto è stato riprovato DOPO lo spostamento**, non dato per buono: due scritture fuori dal recinto, entrambe negate | `.claude/settings.local.json` · misura: `git grep -lE '/User[s]/' -- .` → 0 file | P ha sbloccato, CC ha eseguito | ✅ **chiusa nel giro W1** | — |
| **WD23** | **La pagina pubblicata si dichiara `http://` da sé, e `WA16` non lo risolve.** ⚠️ **Sono due cose diverse:** *Enforce HTTPS* è GitHub che rimanda il traffico (✅ fatto, `301`); il `<link rel="canonical" href="http://aimonx.app/">` lo scrive **Jekyll**, che senza `url` in `_config.yml` indovina lo schema. ⭐ **Il rimedio è già sul ramo:** `_config.yml` dichiara `url: https://aimonx.app`. ⛔ **Ma non è provato**, perché la prova sta oltre il merge: finché `main` non cambia, il mondo legge ancora `http://` | `_config.yml` · misura: `curl -sS https://aimonx.app/ \| grep canonical` | CC | ⏳ **aperta, trovata nel giro W1** — si chiude con la misura DOPO il merge | — |
| **WD24** | **Il recinto copre `Edit`/`Write`/`NotebookEdit`, non `Bash`.** ⭐ **Misurato nel giro W1:** due tentativi di scrittura fuori dal recinto sono stati **negati**, e il recinto funziona per ciò che copre. ⚠️ **Ma nessuna regola nomina `Bash`**, e un comando di shell scrive dove vuole. ⛔ **Non è un buco da tappare di corsa:** le regole `Bash` si scrivono sul testo del comando, non sul percorso toccato, quindi una regola larga blocca lavoro legittimo e una stretta si aggira da sé — ⚠️ **e un controllo che protesta sempre viene disattivato.** ⭐ **Va deciso se vale la pena, non aggiunto per riflesso** | `.claude/settings.local.json` · misura: i due tentativi negati del giro W1 | P | ⏳ **aperta, trovata nel giro W1** | — |
| **WD25** | **Che `_config.yml` tenga davvero fuori i file di lavoro NON È PROVATO, e la prova costa.** ⚠️ **Misurato nel giro W1:** sul Mac c'è Ruby 2.6 di sistema, **niente Jekyll, niente Bundler utilizzabile, niente Docker** — nessuna build locale. ⭐ **Quel che si è potuto provare è stato provato:** il file è **YAML valido** (`ruby -ryaml`), e un `_config.yml` malformato è il modo in cui Pages ricade sui valori di default e pubblica tutto. ⛔ **Resta non provato che Pages onori `exclude`.** ⭐ **Tre strade, coi costi, in consegna del giro W1** | `_config.yml` · misura: `ruby -ryaml -e 'YAML.load_file("_config.yml")'` → valido · `jekyll -v` → non installato | P | ⏳ **aperta, trovata nel giro W1** — ⛔ **precede il merge**, non il push | ⚠️ **da accertare** |
